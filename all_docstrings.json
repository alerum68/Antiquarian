{
  "Archivist/tests\\conftest.py": [
    [
      "module",
      "Makes Archivist.py importable as a plain top-level module (matching how it's run\ndirectly, e.g. `python Archivist.py`) when pytest is run from anywhere.\n\nAlso pins the env constants baked into the golden GEDCOM tests (see\nArchivist/tests/golden/*.ged) BEFORE any module import triggers Utils.py's\nload_dotenv()/os.getenv() reads, so the goldens are byte-identical on any\nmachine regardless of the developer's .env. Plain assignment (not setdefault)\ndeliberately overrides whatever load_dotenv would otherwise load."
    ]
  ],
  "Archivist/tests\\test_archivist.py": [
    [
      "module",
      "Tests for Archivist's semantic-driven role/family-linking logic - the generalization\nthat replaced hardcoded role_number digit checks (role \"2\" is always \"Father\", role \"4\"\nis always the marriage bride, ...) with a small fixed role_semantic vocabulary\n(primary/spouse/child/father/mother/father_in_law/mother_in_law) read directly off each\nparticipant, the same way for any record type."
    ],
    [
      "test_generate_uid_same_person_same_page_matches",
      "Baseline: identical record_id/role/page must always collide onto one UID."
    ],
    [
      "test_generate_uid_same_record_id_different_page_still_matches",
      "Regression: confirmed live, two JSON records for the same real person on different\npages used to hash to different UIDs (page was part of the hash input) - two separate\nINDI blocks for one person. page must no longer affect the UID at all when record_id\n(or lac_pid) is the same."
    ],
    [
      "test_generate_uid_prefers_lac_pid_over_record_id",
      "Once Commissioner attaches lac_pid, it must take priority over record_id (LAC's own\nauthoritative identifier vs. an OCR'd record_number) - proven here by two records with\nDIFFERENT record_ids but the SAME lac_pid still colliding onto one UID."
    ],
    [
      "test_generate_uid_different_record_id_still_differs_without_lac_pid",
      "Sanity check the fix didn't accidentally collapse everything - genuinely different\nclaims (no lac_pid on either) must still get different UIDs."
    ],
    [
      "test_build_general_citation_page_line_uses_claim_affdt_when_present",
      "RootsMagic/FTM auto-name citations from this PAGE line - \"Record EVEN-1964\" (an\ninternal id_prefix+number) made the Citations list unbrowsable in real testing. Use\nthe claim's own real reference numbers instead when known."
    ],
    [
      "test_build_general_citation_single_block_without_source_documents",
      "Backward compatibility: every record type that never populates source_documents\n(everything except Parish/Scrip claims that went through the merge step) must get\nexactly the same single-block behavior as before."
    ],
    [
      "test_build_general_citation_collapses_identical_original_and_translation",
      "Per the user: most Scrip affidavits are already in English, so\ncitation_text and citation_details end up identical - showing both the\n\"English Translation:\"/\"Original Transcription:\" headers in that case just duplicates\nthe same text twice. Only one plain text block, no header labels, when they match."
    ],
    [
      "test_build_general_citation_normalization_is_whitespace_only_not_fuzzy",
      "Word order actually differing (the \"1964\" moved) is a genuine content difference,\nnot just reflow - must NOT collapse, proving the normalization added for whitespace\nreflow doesn't also hide a real mismatch via fuzzy/reordering matching."
    ],
    [
      "test_build_general_citation_collapses_whitespace_only_reflow",
      "The actual confirmed-live case: same words, same order, only newlines vs. spaces\ndiffer - this one must collapse."
    ],
    [
      "test_build_general_citation_keeps_both_blocks_for_a_genuine_translation",
      "The other direction: a real French-original document with a distinct English\ntranslation must keep both labeled sections - nothing to collapse there."
    ],
    [
      "test_build_general_citation_shows_configured_header_labels_when_set",
      "CITATION_DETAIL/CITATION_TEXT are blank by default (no label line) but a user who\nconfigures them must still see their chosen label prefixed - the \"header present\" branch\nof General.citation_text_block, uncovered by every other test in this file (which all run\nagainst the blank-default GENERAL_CONFIG)."
    ],
    [
      "test_build_general_citation_media_only_entry_uses_its_own_path_derived_uid",
      "A Commissioner-downloaded certificate has no transcription, only a media_path -\nits citation block must reference an OBJE built from generate_media_uid_for_path on\nTHAT path, not the shared media_uid passed in for the sheet's own scanned image."
    ],
    [
      "test_generate_media_uid_for_lac_asset_uses_the_e_number_directly",
      "Confirmed live in review: an opaque hashed media UID makes a real GEDCOM harder to\ncross-reference by hand against LAC - the e-number's digits are already a stable,\nunique identifier, so they should be used directly rather than hashed. The leading\n\"e\" is stripped (not kept) - per the user, FTM import breaks on a non-numeric media\nobject ID, so everything after the fixed \"M\" prefix must stay digits-only."
    ],
    [
      "test_build_general_citation_prefers_lac_asset_id_over_hashed_path",
      "The realistic Commissioner shape: a source_documents entry always carries both\nmedia_path AND lac_asset_id - the e-number must win over hashing the path."
    ],
    [
      "test_build_individual_scrip_desc_line_without_claim_or_affidavit_still_shows_scrip_number",
      "No claim_number/affidavit_number present (e.g. an older extraction) - the fact's own\nvalue line still shows whatever Scrip-specific fields it does have."
    ],
    [
      "test_build_individual_scrip_desc_line_excludes_document_type_with_claim",
      "document_type labels ONE physical document (e.g. \"Witness Affidavit\"), not the\nwhole merged claim - showing it in the fact's own value line was misleading (per the\nuser, the note appeared to cite only the witness affidavit). Still available per\ncitation via _TITL's own \"-- {document_type}\" suffix, just not here."
    ],
    [
      "test_build_individual_scrip_desc_line_excludes_document_type_without_claim",
      "Same exclusion in the no-claim/affidavit path."
    ],
    [
      "test_parse_household_forms_second_family_unit_for_unrelated_boarder_household",
      "Regression: a family/dwelling-number group can legitimately contain two unrelated\nfamilies (e.g. a boarder sharing a dwelling with an unrelated nuclear family) - real\n1860 census data ('Depar' family sharing a dwelling with boarder 'Hougton'). Previously,\na plausible spouse pair (matching surname, opposite sex, valid age gap) that didn't\nalso fit as a child of the group's first-listed unit was silently discarded instead of\nforming its own unit - every member of the second family fell through to 'unrelated'\none at a time. parse_household must now recognize the second family as its own unit,\nwith subsequent same-surname children correctly attaching to it."
    ],
    [
      "test_resolve_family_links_baptism_shape_no_spouse_or_children",
      "Primary is purely a child in this record: their parents keep the unsuffixed FAM id,\nexactly as before child/spouse/in-law roles existed."
    ],
    [
      "test_build_individual_primary_with_no_parents_still_gets_famc",
      "Regression: build_family always creates a FAM for a primary who is purely a child\nin this record, even with neither parent present (matching this record type's GEDCOM\noutput from before family/child/in-law roles existed - see resolve_family_links).\nbuild_individual's own FAMC line has to point at that same FAM unconditionally too, not\nonly when has_parents is true - caught by diffing against pre-refactor output on a\nburial with no parents recorded, where this FAMC line went missing entirely."
    ],
    [
      "test_build_individual_fsftid_gets_companion_fs_tree_weblink",
      "Regression: only the record/citation-level FamilySearch ark URL was ever emitted -\nthe person's own FS Tree profile page never had a link of its own, so it couldn't\ncompete with the Ancestry link already present. _FSFTID must now be accompanied by a\nweblink to https://www.familysearch.org/tree/person/details/{fsftid}, RM/FTM-flavored\nthe same way every other weblink already is (weblink_lines)."
    ],
    [
      "test_build_family_burial_with_surviving_spouse_forms_fams_without_marr_event",
      "A burial naming the deceased's surviving spouse is real marriage evidence and should\nlink as a real family - previously dropped entirely (role '4' outside a marriage record\nhad no FAMC/FAMS handling at all). Burial is a person-level fact, so no MARR event is\nattached to this FAM - that event already lives on the deceased's own BURI record."
    ],
    [
      "test_build_individual_scrip_event_gets_type_line_and_value_from_extra_fields",
      "Scrip's own event_type resolves to gedcom_tag 'EVEN' - it's built as a dedicated\n\"Scrip\" custom fact (FactTypes.json code 10004), needing a '2 TYPE Scrip' line for\nRootsMagic to recognize it, with its own Date/Place/Desc all filled in."
    ],
    [
      "test_build_individual_scrip_race_fact_gets_no_document_year_date",
      "Per the user: everything but Race should carry the document year as its DATE -\nRace describes an ongoing characteristic, not something dated to one document."
    ],
    [
      "test_build_individual_scrip_witness_associations_exclude_nuclear_family",
      "Only true witnesses (no family-position role_semantic) become witness\nAssociations - spouse/parent/child participants must not appear here even though the\nold filter (just excluding 'primary') used to sweep them in too. Uses FTM output,\nwhere witnesses render as plain names in a NOTE line - RM's own _SHAR form only ever\ncarries a UID + role text, not a name, so it can't distinguish this directly."
    ],
    [
      "test_build_gedcom_from_general_excludes_commissioner_from_indis",
      "Participants with role_semantic 'commissioner' are captured in JSON for archival reference\nand LAC lookup, but must never be emitted as INDI records or witness associations in GEDCOM."
    ],
    [
      "test_build_individual_baptism_event_gets_no_type_line",
      "A standard GEDCOM-tagged event (Baptism -> BAPM) must not gain a '2 TYPE' line or\nany value text on its '1 BAPM' line - that's exclusively for the 'EVEN' fallback case."
    ],
    [
      "test_build_individual_alternate_name_renders_as_proposed_name_fact_with_event_note",
      "A margin note offering a different spelling (a later annotator's aid, not the\npriest's own entry - see Parish.pmt's MARGIN ANNOTATIONS rule) becomes its own\n'proposed' NAME fact, same convention as census's crowdsourced alternate readings,\nplus a NOTE on the primary's own event so it's visible right where the event is."
    ],
    [
      "test_select_scrip_template_id_certificate_from_document_type_regardless_of_commission",
      "document_type wins over commission_reference - a Certificate is a structurally\ndifferent document from an affidavit no matter which commission issued it."
    ],
    [
      "test_select_scrip_template_id_prefers_series_code_over_commission_reference_text",
      "series_code is LAC's own catalog classification (Commissioner-fetched) - more\nauthoritative than the printed commission_reference text, so it wins when present,\neven when commission_reference text alone would have matched a different template."
    ],
    [
      "test_select_scrip_template_id_returns_none_when_unrecognized",
      "No guessing - an unresolved record must fall back to the plain freeform source,\nnot get mis-templated."
    ],
    [
      "test_scrip_template_field_value_land_grant_fields_are_a_known_gap",
      "No claimant affidavit states these - they belong to a later, separately-cataloged\nDominion Lands Office patent record Commissioner doesn't fetch yet."
    ],
    [
      "test_build_general_citation_scrip_forces_proven_even_when_date_is_estimated",
      "Every Scrip fact is read straight off a sworn primary source - get_proof_status'\ngeneric BEF/ABT/EST downgrade (still correct for Parish/Census) must not apply here."
    ],
    [
      "test_build_general_citation_scrip_falls_back_to_freeform_when_template_unresolved",
      "An unrecognized commission_reference must not get mis-templated - it cites the\nexisting per-volume freeform source instead, same @S{vol}@ id as before this change."
    ],
    [
      "test_build_individual_researcher_citation_has_both_name_and_titl",
      "RootsMagic needs both '2 NAME' and '2 _TITL' under the shared @S1@ researcher\ncitation to merge it across multiple people's citations in its own UI - confirmed\nlive by the user; _TITL alone displays but doesn't merge."
    ],
    [
      "test_apply_record_type_field_remap_hbca_auto_resolves_image_dir",
      "IMAGE_DIR has no user-facing override (not even via env var) - it's always\nauto-resolved to Media/<record type>, matching Paleographer/Extract.py's own\nSOURCE_DIR convention for where that type's images were actually extracted from. A\nstray HBCA_IMAGE_DIR env var (e.g. left over from a legacy .env) must NOT change it."
    ]
  ],
  "Archivist/tests\\test_census_ingestion.py": [
    [
      "module",
      "Tests for build_census_dataframe_from_unified - the adapter translating Voyageur's\nnow-normalized shared-schema census output (sheets[].records[].participants[]) back into\nthe same old-column-named DataFrame shape Archivist's household-parsing/citation logic\nhas always operated on. These functions (parse_household, parse_household_relational,\nget_census_era, etc.) are deliberately NOT changed by this rework - only what feeds them\nchanged - so these tests confirm the adapter's column-naming/grouping produces input\nthose functions still handle correctly, not that the functions themselves changed."
    ],
    [
      "test_adapter_reads_image_id_and_place_details_from_document_metadata",
      "Regression: Image_ID (feeds the GEDCOM's '1 FILE' media path) and Place_Details were\nboth silently dropped by this adapter even when census_schema.py's normalize step\nsupplied them - Image_ID came from document_metadata.file_name (hardcoded '' before the\ncensus_schema.py fix), Place_Details wasn't read into page_meta at all here."
    ],
    [
      "test_adapter_reads_street_and_it_drives_the_cens_addr_line",
      "Confirms 'street' reaches the DataFrame AND actually drives build_gedcom_from_census's\nCENS fact '2 ADDR' line - not just that it lands somewhere plausible-looking."
    ],
    [
      "test_relational_era_household_parsing_works_on_adapted_dataframe",
      "1900 (relationship era) - Head + Wife, explicit role_name - confirms\nparse_household_relational (unchanged) correctly resolves the household from the\nadapter's column naming."
    ],
    [
      "test_heuristic_era_household_parsing_works_on_adapted_dataframe",
      "1860 (heuristic era) - no role_name at all (matches what census_schema.py produces\nwhen the source has no relationship column) - confirms parse_household (unchanged)\nstill infers the spouse pair from age/sex/surname proximity via the adapter's output."
    ],
    [
      "test_dispatch_by_record_type_name_not_shape",
      "Both census and church documents use the \"sheets\" top-level key now - confirms\nis_census's own logic (record_type_name-based, not a hardcoded shape guess) would\nroute a Census_-prefixed document to the census flavor."
    ],
    [
      "test_build_census_task_folder_name_uses_fixed_vocabulary_not_raw_flag_text",
      "Regression: RootsMagic rejects an arbitrary sentence as a \"0 _FOLDER\" value -\nbuild_census_task used to write one directly (a lightly regex-cleaned copy of the raw\nreview-flag text, e.g. \"Head-surname match: no age fit\"). The folder name must instead\ncome from evaluate_task_priority's fixed, safe vocabulary - already proven correct for\nthe general flavor - reusing the exact flag text real 1860 census data produced."
    ],
    [
      "test_census_gedcom_output_has_no_illegal_name_under_sour_and_single_extension",
      "Regression for the FTM import-error root cause (confirmed against a real FTM import\nerror log: ~85 \"Unsupported or invalid tag: NAME\" errors on one real test census page):\na \"NAME Researcher: ...\" line was being written as a child of a per-fact \"2 SOUR ...\"\ncitation, which isn't legal under GEDCOM 5.5.1 (see build_census_citation). This is\ndistinct from \"2 NAME Researcher: ...\" under the shared \"1 SOUR @S1@\" researcher\nreference on every individual - confirmed live by the user that RootsMagic needs BOTH\n\"2 NAME\" and \"2 _TITL\" there to merge that citation across every person who cites it;\nthat pattern is intentional, not a regression of this fix. Also regresses an OBJE media\nfilename doubling its extension (\".jpg.jpg\") when Image_ID already carries one, as it\ndoes coming from the unified schema's document_metadata.file_name - and confirms the\nFamilySearch Family Tree profile webtag now accompanies _FSFTID."
    ],
    [
      "test_census_gedcom_media_attaches_to_each_fact_citation_per_original_design",
      "This project's own citation-building shape, unchanged since the very first commit\n(CensusConverter/CensusConverter.py, build_citation_block): the OBJE media reference\nlives INSIDE each fact's own '2 SOUR ...' citation block (at '3 OBJE <id>', a child of\nthe citation, not a direct child of the fact) - and that whole citation block, media\nincluded, is what gets attached once per fact (NAME, BIRT, CENS, OCCU, ...). A session\nmid-course attempt to instead park the media once on the shared top-level SOUR record\nwas reverted after review - confirmed against project history that per-fact-citation\nattachment (not source-only) is the long-standing, correct design."
    ],
    [
      "test_census_gedcom_refn_is_bare_ark_not_the_gedcomx_type_prefixed_form",
      "User-requested: a FamilySearch person ark's own REFN value should read as just the\nark itself (e.g. 'MF36-Z6D'), not the GEDCOM X type-prefixed form ('1:1:MF36-Z6D') that\nFS.py's pid/person_ark actually carries and that the citation-level _FSFTID fallback\nalso strips (see test_census_ingestion.py). The '@I<id>@' cross-ref pointer itself is\nuntouched - that's an internal GEDCOM pointer needing only internal consistency, not\ndisplay-cleanliness, and stripping it would risk mismatching its own definition/\nreference pair."
    ],
    [
      "test_census_citation_never_emits_apid_for_an_ark_shaped_rec_id",
      "Regression: an FS-only gather has no real Ancestry APID_DB, but rec_id (fed from\nFS.py's per-person 'pid', which is the FamilySearch person ark, e.g. '1:1:MF36-Z6D'\nwhen there's no genuine Ancestry record) was still being unconditionally baked into the\nAncestry-specific '_APID 1,<db>::<id>' GEDCOM tag - an ark is not an APID and must\nnever appear there, and no bogus Ancestry citation data should be emitted at all.\n\nThe ark itself must NOT be routed into the top-level per-person _FSFTID/tree-weblink\nmechanism (an earlier fix attempt did this, then had to be reverted - confirmed live it\nproduced a broken 'familysearch.org/tree/person/details/1:1:MF36-Z6D' weblink, since\nthat URL pattern is specifically for a Family Tree PROFILE id, a different, unrelated\nID namespace only obtained via genuine tree-attachment). This test's row has no\nFamilySearch_URL either (not FS-sourced at all), so the citation-level _FSFTID fallback\n(see the companion test below) correctly doesn't fire - _FSFTID must not appear."
    ],
    [
      "test_census_citation_fsftid_falls_back_to_bare_ark_for_familysearch_sourced_rows",
      "User-requested follow-up: the citation-level _FSFTID field should carry this record's\nown FamilySearch person ark - as an identifier, not a URL - when there's no genuine\ntree-attached FSFTID, but ONLY for rows we already know are FamilySearch-sourced\n(FamilySearch_URL present); never fabricated for Ancestry-only rows. And it must be\nBARE (no '1:1:' GEDCOM X type prefix) - that prefix stays only in the actual clickable\nweblink, which is unaffected here."
    ],
    [
      "test_census_citation_household_id_field_is_bare_number_when_only_one_number_present",
      "Regression: RM's 'HouseholdID' citation FIELD was rendering as 'family 1' or\n'dwelling 5' (confirmed live against a real 1860 gather - RootsMagic shows\n'HouseholdID: family 1', which reads as a stray descriptive word next to the field's\nown name) instead of the bare id value. The label word only earns its place when BOTH\ndwelling and family numbers exist and genuinely need to be told apart."
    ],
    [
      "test_census_citation_still_emits_apid_for_real_ancestry_data",
      "Companion to the regression above - a genuine Ancestry-sourced record (real APID_DB,\nnumeric rec_id, no FSFTID) must still get its _APID tag exactly as before."
    ],
    [
      "test_dynamic_occupation_template_normalizes_raw_case",
      "Regression: get_occupation_value's 2026-08-15 refactor dropped the cap_case\nnormalization the pre-refactor version applied to its assembled result - real census\nsource text can come back ALL-CAPS or lowercase, unlike this test file's other\nalready-Title-Case fixtures."
    ],
    [
      "test_canadian_and_historical_hbc_birthplaces_are_not_foreign",
      "Regression: this project's core subjects are Canadian/M\u00e9tis/HBCA-region families\n(see Gazetteer.CA_PROVINCE_NAMES) - is_foreign_birthplace()'s original\nUS_STATES_AND_TERRITORIES allowlist had no Canadian entries at all, so every\nCanadian- or Rupert's-Land-born ancestor would have had their raw birthplace text\ndumped verbatim into a NATI tag as if it were a foreign nationality."
    ]
  ],
  "Archivist/tests\\golden\\capture_golden_gedcom.py": [
    [
      "module",
      "Golden-file capture: runs build_gedcom_from_general on today's unmodified\nArchivist.py against a Scrip fixture and a Parish fixture, for both target\nsoftware flavors, and writes the four outputs as committed .ged fixtures.\nTask 6's regression test rebuilds the same fixtures through the post-split\nmodules and diffs byte-for-byte against these files - run this script again,\nby hand, ONLY if a real (intentional) behavior change is made after the split;\nnever re-run it to make a failing regression test pass."
    ]
  ],
  "Commissioner/tests\\conftest.py": [
    [
      "module",
      "Makes `Commissioner` importable as a package (e.g. `from Commissioner import models`)\nwhen pytest is run from anywhere, the same way every other module's tests/conftest.py\nputs its own module on sys.path."
    ]
  ],
  "Commissioner/tests\\test_fact_registry.py": [
    [
      "test_export_fact_types_json_matches_real_file_on_disk",
      "Guardrail: Archivist.py, Paleographer.py, engine.py, Voyageur.py, and FS.py\nall still read the real FactTypes.json directly. If Commissioner.models'\nFACT_DEFINITIONS ever drifts from that file, this must fail immediately."
    ]
  ],
  "Commissioner/tests\\test_models.py": [
    [
      "test_models_match_schema_json_field_names",
      "Guardrail: Paleographer still reads schema.json directly for its LLM calls.\nIf models.py's fields ever drift from that file's shape, this must fail\nimmediately rather than silently diverging."
    ],
    [
      "test_models_match_schema_json_field_descriptions",
      "Guardrail: field names alone can't catch a dropped description, and the\ndescriptions ARE the LLM-facing guidance - losing one silently degrades extraction\nquality with no other symptom."
    ],
    [
      "test_expected_descriptions_still_matches_real_schema_json",
      "The EXPECTED_DESCRIPTIONS literals above are the contract; this proves they are\nstill a faithful copy of the real schema.json Paleographer ships, so the guardrail\ncan't quietly pass against a stale expectation."
    ],
    [
      "test_only_participant_fields_are_required",
      "schema.json's only `required` list is on Participant's item schema; every other\nfield is modeled as optional. Record/Sheet must not be stricter than the source of\ntruth, or real extractions that omit e.g. page_id would fail validation."
    ]
  ],
  "Commissioner/tests\\test_package.py": [
    [
      "test_commissioner_package_imports",
      "Baseline: the package must be importable and pydantic must be available,\nbefore any real models exist."
    ]
  ],
  "Commissioner/tests\\test_record_registry.py": [
    [
      "test_census_record_extra_fields_include_place_details",
      "Regression: place_details is set by census_schema.normalize_census_pages() (one\nof the loc_key fields carried through from every page) but was never declared here,\nso any record carrying it would fail Commissioner validation with extra_forbidden."
    ],
    [
      "test_census_participant_extra_fields_include_all_ancestry_fs_mapped_keys",
      "Regression: confirmed live (2026-08-15 Canadian gather) that marital_status alone\ntripped a real Commissioner extra_forbidden error, even though ancestry_census.yaml\nhas mapped it since the original Ancestry index-panel-data plan's Task 4. Auditing\nevery participant_fields type_specific_fields.* target across both\nancestry_census.yaml and familysearch_census.yaml (plus census_schema.py's own\nalways-added passthrough keys) surfaced four more of the same gap: street,\nmarried_within_year, birth_month, and alternate_birth_places."
    ],
    [
      "test_undeclared_extra_field_key_is_rejected_not_ignored",
      "extra='forbid': since the validated model is never written back over the caller's\ndict, an undeclared key must fail loudly rather than be silently ignored."
    ],
    [
      "test_build_registry_accepts_a_valid_fixture_dir",
      "Control for the test above: same fixture with a supported type token loads fine,\nso the failure there is the type token and not the fixture format."
    ],
    [
      "test_parse_collection_leaves_type_specific_fields_exactly_as_given",
      "parse_collection validates type_specific_fields; it must never rewrite them.\nOverwriting with the validation model's dump would inject None for every\ndeclared-but-absent field - silent data corruption of the caller's payload."
    ],
    [
      "test_parse_collection_preserves_empty_type_specific_fields_for_parish",
      "Parish.pmt declares zero extra_fields, so an overwrite-with-dump would have been\ninvisible here - but every Parish record's dict must still survive untouched."
    ]
  ],
  "Paleographer/tests\\conftest.py": [
    [
      "module",
      "Makes engine.py/postprocess.py importable as plain top-level modules (matching\nhow Paleographer.py itself imports them) when pytest is run from anywhere."
    ]
  ],
  "Paleographer/tests\\test_agy_engine.py": [
    [
      "module",
      "Tests for agy_engine.py. agy_client.call_agy_structured is monkeypatched throughout -\nthese tests are about agy_engine's own scratch-dir staging, retry policy, and cost\nadaptation, not the subprocess layer (already covered in\nAntiquarianMCP/tests/test_agy_client.py). rasterize_pdf_to_images is tested against a\nreal, tiny, locally-generated PDF (via PyMuPDF, already a project dependency) since\nthat logic is cheap to exercise for real rather than mock."
    ],
    [
      "test_call_agy_extract_chunked_threads_continuation_across_chunks",
      "A record cut off at the end of chunk 1 that chunk 2 claims to continue must end\nup as ONE record (chunk 2's, since that's the completed version), not two."
    ],
    [
      "test_call_agy_extract_chunked_falls_back_when_consolidation_fails",
      "Confirmed live: consolidation can fail after retries even when every individual\nchunk succeeded (reasoning over the whole combined result at once hits the same\nscale limit chunking exists to avoid). That must never discard the real,\nsuccessfully-extracted per-chunk data - it must fall back to the unconsolidated\nresult instead of raising."
    ],
    [
      "test_call_agy_extract_chunked_saves_leftover_when_last_chunk_ends_mid_record",
      "If the very last chunk's last record is cut off with no further section to check\nagainst, it must still be saved, not silently dropped."
    ]
  ],
  "Paleographer/tests\\test_crosscheck.py": [
    [
      "module",
      "Unit tests for Paleographer.py's cross_check_claim_record - wired into the crosscheck CLI\nmode by this same cleanup pass (see Paleographer.py main()'s \"crosscheck\" dispatch branch).\nNo real network call is ever made: lac_client.search and voyageur_lac.download_pid_bundle\nare monkeypatched on the imported module's own attributes."
    ]
  ],
  "Paleographer/tests\\test_engine.py": [
    [
      "_write_fact_types_fixture",
      "event_types now always comes from the shared FactTypes.json (see\nengine.load_event_types), not from a .pmt's own front matter, so tests point\nengine.FACT_TYPES_PATH at a small controlled fixture instead of the real project file."
    ],
    [
      "test_parse_type_config_defaults_role_validation_to_closed",
      "Matches Commissioner.record_registry's own default (role_validation_mode =\nfront_matter.get(\"role_validation\", \"closed\")) - a .pmt with no role_validation key\nat all must resolve to closed, not open."
    ],
    [
      "test_build_vocabulary_summary_closed_mode_uses_choose_exactly_one_phrasing",
      "Regression test for existing Parish/Scrip behavior - role_validation: closed (or\nunset) must keep the original 'choose exactly one' phrasing unchanged."
    ],
    [
      "test_build_vocabulary_summary_open_mode_uses_escape_hatch_phrasing",
      "Census.pmt's open mode (Fix 3): the model must be told the role list isn't\nexhaustive and that anything else is recorded verbatim as an association, never\ncoerced into one of the listed family roles."
    ],
    [
      "test_build_merged_schema_maps_dict_field_type_to_json_schema_object",
      "Census.pmt declares `unmapped: {type: dict}` - \"dict\" is not a valid JSON-Schema\ntype (the correct token is \"object\"), so inject() must translate it rather than pass\nthe raw front-matter token straight through (Fix 1)."
    ]
  ],
  "Paleographer/tests\\test_master_db_merge.py": [
    [
      "module",
      "Confirms get_processed_files/merge_sheets correctly treat Voyageur-written scaffold sheets\n(document_metadata present, every record's participants empty) as unprocessed, and that\nmerge_sheets replaces a placeholder sheet with Paleographer's own real AI-filled sheet for\nthe same file_name in place, instead of appending a duplicate - see the\nVoyageur-Parish-Scrip-scaffold design spec's Architecture section (Fix 1a/1b)."
    ]
  ],
  "Paleographer/tests\\test_paleographer_pipeline.py": [
    [
      "module",
      "End-to-end simulation of Paleographer.py's full pipeline: file discovery -> content-part\nbuilding -> prompt/schema assembly -> (fake) AI Assistant call -> post-processing -> master DB\nwrite. No real API call is ever made - google.genai.Client is replaced with a fake that\nreturns a canned, schema-shaped response - but every other piece (the real Parish.pmt/\nScrip.pmt prompt files, the real FactTypes.json vocabulary, engine.py's schema merging,\npostprocess.py's derivation logic, Paleographer.py's own orchestration) runs for real.\n\nThis exists because engine.py and postprocess.py already have thorough unit tests for\ntheir own pieces in isolation, but nothing previously exercised Paleographer.py itself or\nverified the pieces actually work together end-to-end for either real record type."
    ],
    [
      "FakeModels",
      "fake_page_data is either one dict (returned for every call, the common case) or a\nlist of dicts (consumed one per call, in order - for simulating a multi-file run where\neach image needs its own distinct response, e.g. page-continuation tests)."
    ],
    [
      "FakeClient",
      "Stands in for genai.Client so process_one_file_sync's real logic (retry loop,\nJSON parsing, cost tracking, context caching) runs unchanged against a canned\nresponse instead of a real network call."
    ],
    [
      "_media_type_name",
      "TYPE_CFG.name equals the active .pmt file's stem (engine.parse_type_config sets\nname=pmt_path.stem), and Extract.py scans <GENEALOGY_DIR>/Media/<TYPE_CFG.name> for new\nimages (SOURCE_DIR). Parish.pmt -> \"Parish\", Scrip.pmt -> \"Scrip\" - the fixtures must\nstage their images under the same subfolder the active record type resolves to."
    ],
    [
      "test_build_merged_schema_maps_dict_field_type_to_json_schema_object",
      "Paleographer.py's own copy of build_merged_schema/inject() (duplicated from\nengine.py) must also translate a `dict` field type to JSON-Schema's `object` - Fix 1\nof the final-review fix brief. Uses a synthetic core schema, same as engine.py's own\nunit test, rather than exercising the full pipeline."
    ],
    [
      "test_build_vocabulary_summary_closed_mode_matches_parish_pmt",
      "Regression test for Parish.pmt's existing closed-mode phrasing - must stay\nbyte-for-byte unchanged from before Fix 3."
    ],
    [
      "test_build_vocabulary_summary_open_mode_matches_census_pmt",
      "Census.pmt declares role_validation: open (Task 2) - Paleographer.py's own prompt\ngenerator must produce the escape-hatch phrasing, not force-fit a boarder/lodger into\none of the 9 family roles (Fix 3, the most important finding)."
    ],
    [
      "test_debug_mode_does_not_write_master_db",
      "DEBUG_FILE mode (the Toolbox's 'Run Analysis (API)' with a specific file picked)\nmust print the extracted JSON without ever touching the master DB on disk."
    ],
    [
      "test_page_continuation_merges_across_images_without_duplication",
      "A record cut off at the bottom of one image and completed at the top of the next\nmust end up as ONE record in the master DB, not two - see UNIVERSAL_PROMPT_SUFFIX's\nPAGE CONTINUITY rule, engine.build_continuation_context, and Paleographer.py's\npop_trailing_cutoff_record/consumed_leading_continuation/reattach_leftover_record."
    ],
    [
      "test_page_continuation_saves_leftover_when_nothing_continues_it",
      "If the next image's response doesn't claim to continue the pending record, it must\nstill be saved (exactly as flagged as before this feature existed), not silently\ndropped."
    ],
    [
      "test_agy_engine_stages_multiple_rasterized_pages_for_pdf",
      "A PDF should be rasterized to multiple images (mocked here as 3) and all of them\npassed to call_agy_extract in one call, not split into separate per-page calls."
    ]
  ],
  "Paleographer/tests\\test_schema.py": [
    [
      "module",
      "Structural validation of hand-written fixture documents against the merged\nschema. Fixtures avoid null values throughout: AI Assistant's \"nullable\" extension\nisn't consulted by the standard jsonschema library the way it is by the\nlive API, so a fixture testing that relaxation would need a custom\nvalidator; this test is a structural smoke check, not a full contract test."
    ]
  ],
  "Paleographer/tests\\test_settings_standalone.py": [
    [
      "module",
      "Confirms Paleographer.py resolves its own runtime settings (SOURCE_DIR, MASTER_DB) from\nonly its own record type's PREFIXED settings-tab keys (e.g. CHURCH_IMAGE_DIR,\nCHURCH_MASTER_DB_NAME) via field_remap - with the generic names (IMAGE_DIR,\nMASTER_DB_NAME) never set at all. This is the actual standalone-execution scenario: no\nAntiquarian.py subprocess-env injection bridging prefixed names to generic ones."
    ]
  ],
  "Voyageur/tests\\conftest.py": [
    [
      "module",
      "Makes FS.py importable as a plain top-level module (matching how it's run directly)\nwhen pytest is run from anywhere."
    ]
  ],
  "Voyageur/tests\\test_a.py": [
    [
      "module",
      "Tests for A.py's census-gather normalization call site (Issue #3 test-coverage gap)."
    ],
    [
      "test_normalize_ancestry_census_gather_derives_canadian_title_from_page_country",
      "Regression: confirmed live (2026-08-15, dbId 1578, Ontario) that collection_title\nhardcoded \"US Federal Census\" regardless of the gather's own country - the title must\ninstead reflect whatever Voyageur.js's own ancestryCountryFromState() recorded on the\nraw page."
    ]
  ],
  "Voyageur/tests\\test_census_schema.py": [
    [
      "module",
      "Tests for census_schema.py's field-map normalization across all three census eras."
    ],
    [
      "test_validate_against_commissioner_survives_broken_commissioner_import",
      "The Commissioner.record_registry import happens inside validate_against_commissioner's\nown try block (Fix 2), not at census_schema's module scope - so even if\nCommissioner.record_registry itself is unimportable/broken (e.g. a malformed .pmt file\nraising inside _build_registry() at import time), this function must still catch it\nand warn rather than let the import propagate and crash A.py/FS.py at startup."
    ],
    [
      "test_citation_carries_collection_id_from_page_for_source_id_resolution",
      "FS.py's build_census_json attaches the FamilySearch catalog collection number (the\n'CC' from ?cc=<id> in the collection URL) as page['collection_id'] so Archivist's\nCensus.py can use it as CENSUS_SOURCE_ID instead of an auto-assigned registry id -\nthis dropped silently when the shared citation dict below didn't copy it over."
    ],
    [
      "test_citation_prefers_parsed_collection_name_and_url_over_raw_document_title",
      "FS.py's citation parser (parse_citation) produces a clean collection name/url from\nFamilySearch's own generated citation prose - build_census_json now carries those onto\npage['collection_name']/['collection_url']. This must win over the collection_title\nparam passed in here, which is the raw document.title (often 'Title; ark URL' shaped\non FamilySearch collection pages) - using it directly was the source of a corrupted\ncitation.collection_name in production."
    ],
    [
      "test_document_metadata_file_name_sanitizes_familysearch_ark_image_id",
      "Regression: document_metadata.file_name was hardcoded to '' - Archivist's\nbuild_census_dataframe_from_unified reads exactly this field into the DataFrame's\nImage_ID column (Census.py:1403), which then feeds the GEDCOM's '1 FILE' media path.\nWith it empty, every census GEDCOM (both sources) silently pointed OBJE records at a\nfilename that doesn't exist on disk (confirmed live: '_00001.jpg' instead of the real\nsaved '3_1_33S7-9YBJ-9PD7.jpg'/'4211353_00001.jpg') - RootsMagic/FTM would show every\ncensus image as missing. FS's raw image_id is an unsanitized ark ('3:1:33S7-9YBJ-9PD7')\nand must be sanitized the same way FS.py's own sanitize_item_id_filename does for the\nnon-census path, to match what's actually on disk."
    ],
    [
      "test_type_specific_fields_carries_place_details_through",
      "Regression: Ancestry's page-level place_details (leftover browsePath segments beyond\nstate/county/city/ED, e.g. a specific street/ward/precinct) was silently dropped by the\nunified-schema path - present in page but never copied into type_specific_fields."
    ],
    [
      "test_participant_carries_alternate_birth_places_through",
      "Regression: Ancestry's per-person alternate_birth_places (real data - readPersonAlternates()\nin Voyageur.js) was silently dropped - _normalize_participant's initial dict set\nalternate_names at the top level (correctly read by Census.py's row['AlternateNames'])\nbut never set alternate_birth_places anywhere, even though Census.py's unified-path\nadapter reads it from type_specific_fields (pts.get('alternate_birth_places', [])) -\nbuild_alternate_birth_lines() silently never fired for any unified-path gather."
    ],
    [
      "test_married_within_year_is_a_per_participant_field_not_dropped_as_a_record_field",
      "Regression: 'Married within Year' was categorized as a record_field in\nancestry_census.yaml, which only handles household/dwelling-grouping keys\n(_household_key only reads family_number/dwelling_number targets) - its own value was\nnever actually copied anywhere, just marked 'consumed' so it wouldn't get flagged as\nunmapped. Census.py's build_gedcom_from_census reads it PER PERSON (row.get('Married\nwithin Year') on the head/wife rows specifically) to decide whether to emit a '1 MARR'\nfact - it needed to be a participant_fields target, not a record_fields one."
    ],
    [
      "test_ancestry_birth_month_and_marital_status_are_mapped_not_unmapped",
      "Regression for the two new participant_fields entries this session's Ancestry\nindex-panel-data investigation surfaced (1880's SelfBirthMonth, 1880/1920's\nSelfMaritalStatus) - confirms they land in type_specific_fields and do NOT trigger\nthe unmapped-column review flag."
    ],
    [
      "test_ancestry_religion_and_nationality_are_mapped_facts_not_unmapped",
      "Regression for this plan's Task 2 - Religion and Nationality are real,\npre-existing FactTypes.json fact types (confirmed via Task 2 Step 1's live check),\ncommon on Canadian census years and previously absent from ancestry_census.yaml\nentirely. Confirms both land as facts, not flagged for manual review."
    ],
    [
      "test_street_address_is_mapped_and_house_number_is_not_double_claimed",
      "Regression: 'Street'/'Street Address'/'Address' were unmapped in\nancestry_census.yaml's participant_fields - Census.py's build_gedcom_from_census\nalready reads row['Street'] to build a CENS fact's '2 ADDR' line, but the raw value\nnever reached it. Deliberately NOT mapping 'House Number' to the same target here -\nthat header is already claimed by record_fields as a dwelling_number alias (a\ndifferent census concept, the sequential dwelling-visited count), so it must not also\nresolve to a street value."
    ],
    [
      "test_group_household_prefers_household_id_over_column_based_key",
      "Ancestry's index-panel-data API supplies a real, stable household_id per person\n(Task 1/2/3 of docs/superpowers/plans/2026-08-15-ancestry-index-panel-extraction.md) -\nwhen present, it must win over the existing Family/Dwelling Number column-based\ninference, since it's a direct signal from Ancestry itself rather than a guess from\ncolumn text that can vary or be absent by census year. Two people share a\nhousehold_id but have DIFFERENT Family Number column values (simulating a data\ninconsistency) to prove household_id, not the column, decides the grouping."
    ],
    [
      "test_group_household_falls_back_to_column_based_key_when_household_id_absent",
      "The DOM-table-scraping fallback path (Task 3) never sets household_id - confirms\nthe existing column-based grouping still works completely unchanged when it's\nabsent, matching every pre-existing test in this file."
    ]
  ],
  "Voyageur/tests\\test_fs.py": [
    [
      "module",
      "Tests for FS.py's build_universal_json() document_metadata fix and Commissioner\nvalidation wiring - see the Voyageur-Parish-Scrip-scaffold design spec."
    ],
    [
      "test_build_census_json_accepts_household_view_row_shape",
      "Locks in this design's core claim: the household-view scraper (replacing the old\nImage Index table scraper) can produce rows in this exact shape with zero FS.py/Census.py\nchanges - see docs/superpowers/specs/2026-08-07-familysearch-household-view-gather-design.md.\nColumn keys and values mirror what a real View Name panel showed live (Joseph Rolette\nhousehold, 1850 Minnesota census): Given Name/Surname split from Essential Information,\nGender from \"Sex: M\", Relationship to Head from Household Details (\"Spouse\"/\"Child\"),\nFamily Number synthesized per household section (not from FamilySearch, which has no\nsuch field)."
    ]
  ],
  "Voyageur/tests\\test_gather_helpers.py": [
    [
      "test_resolve_census_image_dir_absolute_base",
      "Regression: a \" - \"-joined location_folder (state - county - city, the exact\nshape A.py's/FS.py's own filename-derived location text arrives in) must nest as\nreal subfolders (State\\County\\City), not one folder literally named with embedded\n\" - \" text."
    ],
    [
      "test_resolve_census_image_dir_nests_state_county_city",
      "Confirms the full 3-level nesting a real gather's location text produces (e.g.\nVoyageur.js's own \"Michigan - Kent County\" browsePath-derived location string)."
    ],
    [
      "test_census_collection_folder_name_generic_country_no_collection_name",
      "Not a fixed US-or-Canada choice - any country plugs into the same \"{year}\n{country} Census\" template, uniform format, no country list to maintain."
    ],
    [
      "test_census_collection_folder_name_prefers_real_collection_name",
      "The gather's own real collection_name (Ancestry's scraped citation text, or FS's\nown collection_title) beats the generated template when one was actually captured -\nneeds no country-detection logic of its own to be correct for any country."
    ]
  ],
  "Voyageur/tests\\test_hbca_gather.py": [
    [
      "test_hbca_image_dir_reads_from_env",
      "HBCA_IMAGE_DIR was a hardcoded literal, never actually read from the environment,\ndespite being exposed as a configurable setting in settings_schema.yaml - the GUI\ncontrol had no effect. Reloading the module fresh with the env var set is required\nsince HBCA_IMAGE_DIR is resolved once at import time, same as the file's other\nmodule-level settings constants."
    ],
    [
      "test_gather_hbca_sheets_one_entry_failure_does_not_crash_the_batch",
      "Regression test: process_entry ran inside a ThreadPoolExecutor with no try/except\naround its own download call, so one entry's network exception used to propagate out\nof future.result() and crash the entire multi-threaded gather."
    ],
    [
      "test_gather_hbca_sheets_serializes_keystone_media_downloads_across_threads",
      "Two entries whose Keystone resolution names an overlapping media_url must not race\non the same destination file - download_keystone_media's existence-check-then-write\nwas not synchronized, unlike the correctly-locked MASTER_DB/checkpoint path. Proven via\na concurrency counter around the real download_keystone_media call: with two threads\nboth wanting to download the same media_url, the max-concurrent count must stay at 1 -\na real race (unsynchronized) would let both threads observe \"not yet downloaded\"\nsimultaneously and let the counter hit 2, since each call sleeps briefly to force\noverlap if nothing is serializing them."
    ],
    [
      "test_gather_hbca_sheets_one_entry_write_failure_does_not_crash_the_batch",
      "Regression test: the first fix round only wrapped the network GET in try/except,\nleaving the atomic_write_bytes() call right below it unguarded - a write-side failure\n(e.g. disk full) still propagated through future.result() and crashed the whole batch,\nexactly the failure mode the GET-side fix claimed to have eliminated. Caught by\n/code-review before this landed."
    ]
  ],
  "Voyageur/tests\\test_hbca_keystone.py": [
    [
      "test_keystone_query_cache_write_is_atomic",
      "A crash mid-write must never leave the cache file corrupted/truncated - same\natomic-write discipline as save_checkpoint/append_scaffold_sheet elsewhere in HBCA.py.\nThis path is unreachable in production today (gather_hbca_sheets never passes\ncache_file to query_keystone_for_code), but is exercised directly by this test."
    ]
  ],
  "Voyageur/tests\\test_lac.py": [
    [
      "module",
      "Tests for LAC.py's Commissioner-scaffold building blocks: MASTER_DB path resolution\n(mirroring Paleographer.py's own resolve_setting), load/save, and scaffold-sheet\ndeduplicated append. See the Voyageur-Parish-Scrip-scaffold design spec."
    ],
    [
      "_FakeProcess",
      "Synchronous stand-in for multiprocessing.Process - runs the worker entrypoint\nin-process instead of spawning a real subprocess, so tests can drive real SUCCESS/\nSTART/FAIL messages through the controller's queue-consuming logic without the\npickling/spawn complications (or real concurrency) of an actual child process.\nis_alive() is False by construction: start() already ran the worker to completion\nsynchronously before returning, so there's never a moment this fake is still \"running\"."
    ],
    [
      "test_process_worker_messages_updates_to_latest_message_per_worker",
      "Regression test for the exact race #18 found: if a worker's SUCCESS(A) and\nSTART(B) both land in the same drain batch, active_workers must reflect B (the\nworker's actual current task) afterward, not stall on A's now-irrelevant state -\notherwise a watchdog check right after could time out the worker over a task (A)\nit already finished, while the task it's ACTUALLY running (B) stays invisible and,\nif the watchdog fires, gets killed with no requeue and no failure record."
    ],
    [
      "test_download_volume_assets_multiworker_flushes_partial_progress_on_crash",
      "Caught by /code-review: batching removed the old code's per-event flush, which was\nthe only thing guaranteeing a crash mid-harvest didn't lose already-downloaded PIDs'\ncheckpoint/master_db state. With _FakeProcess draining every task synchronously before\nthe controller loop even starts, all 5 PIDs' SUCCESS messages land in one drain batch -\nforcing append_scaffold_sheets to raise on the 3rd one simulates an unexpected crash\npartway through processing that batch. The first 2 PIDs' in-memory state was already\nmutated before the raise; a bare \"while ... finally: flush\" must still persist it."
    ],
    [
      "test_download_images_does_not_claim_success_when_some_canvases_failed",
      "Caught by /code-review: the completion message printed unconditionally, right after\nthe failure warning, misleadingly claiming \"completed successfully!\" even when the\nfailed dict this same diff added already had the data to know better."
    ]
  ],
  "tests\\conftest.py": [
    [
      "module",
      "Makes Antiquarian.py importable as a plain top-level module (matching how it's run\ndirectly, e.g. `python Antiquarian.py`) when pytest is run from anywhere."
    ]
  ],
  "tests\\test_code_quality.py": [
    [
      "module",
      "Automated code quality and lint suite matching PyCharm inspections.\nRuns flake8 (pycodestyle + pyflakes) programmatically across all modules."
    ],
    [
      "test_code_quality_flake8",
      "Verify that flake8 (pycodestyle + pyflakes) reports 0 errors across the codebase."
    ]
  ],
  "tests\\test_load_tool_schema.py": [
    [
      "_restore_shared_dicts",
      "_load_tool_schema mutates shared module-level dicts as a side effect - snapshot and\nrestore them around every test so tests can't pollute each other or the real schema\nstate loaded at import time."
    ]
  ],
  "tests\\test_antiquarian_voyageur_settings_ui.py": [
    [
      "test_visible_sections_for_lac_does_not_include_ancestry_only_settings",
      "GATHER_ON_COLLISION only means anything to A.py/FS.py - LAC.py never reads it, so\nit must not leak into LAC's section just because Gather Settings is always included."
    ],
    [
      "_all_widget_texts",
      "Collects every widget's .cget(\"text\") found anywhere under the scrollable form\ncontainer - both section headers and field labels are plain CTkLabels, no dedicated\nwidget type to distinguish them, so this walks the whole tree and returns everything."
    ],
    [
      "test_lac_source_does_not_show_the_ancestry_only_collision_setting",
      "Regression test for the exact bug a review caught: an earlier version of this\nfeature put GATHER_ON_COLLISION in the always-shown Gather Settings section, which\nmade it appear for LAC even though LAC.py never reads that env var."
    ]
  ]
}