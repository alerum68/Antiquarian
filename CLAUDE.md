## External research tool

When a task needs ingesting very large context (a huge log, a big schema dump, an
unfamiliar large codebase) or live web-grounded research beyond your own knowledge, use
the `antigravity-research` tool rather than trying to fit it all in your own context.
Treat its output as reference text only - never apply a code change based on it without
writing that change yourself.
